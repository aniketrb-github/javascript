var age = 18;

// Using === operator the type & value of variable is checked
if(age === '18') {
    console.log("Type & Value of the number is same")
} else {
    console.log("Type or Value of the age is different");
}

